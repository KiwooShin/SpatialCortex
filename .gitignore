# Python
__pycache__/
*.pyc
.venv/
venv/

# Data (generated, too large for git)
data/*.json
data/*.ply
data/*.faiss
data/*.sqlite
data/*.mp4

# COLMAP output
data/colmap/
data/colmap_*/

# 3DGS training output (large .ply, renders, etc.)
data/gaussian_output/

# Keyframes are tracked (small JPEGs for demo)
# data/keyframes/ is intentionally NOT ignored

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Raw sensor data
*.vrs
*.csv.gz
*.rrd

# Model inference output (large CSVs, JPEGs, pickle caches)
output/

# AEO dataset sequences
data/aeo/

# Dataset download manifests (contain signed CDN URLs — do not commit)
AEO_download_urls.json
*_download_urls.json
